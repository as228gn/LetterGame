<a href="admin/create">Lägg in bild</a>
<a href="./">Spel</a>

  <table>
    <thead>
        <tr>
            <th>Bild</th>
            <th>Rätt svar</th>
            <th>Ta bort</th>
        </tr>
    </thead>
    <tbody>
      <% for (const image of images) { %>
        <tr data-id="<%= image.id %>">
          <td>
            <img id="adminPicture" src="<%= image.imageUrl %>" alt="picture">
          </td>
          <td>
            <%= image.correctAnswer %>
          </td>
          <td>
            <a href="admin/<%= image.id %>/delete">Ta bort</a>
          </td>
        </tr>
        <% } %>
    </tbody>
</table>